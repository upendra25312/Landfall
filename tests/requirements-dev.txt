# Test-only dependencies. The ingestion logic under test also needs `openpyxl`
# (already in src/api/requirements.txt).
pytest>=8,<9
openpyxl>=3.1
python-docx>=1.1
python-pptx>=1.0
fastapi>=0.115,<0.116     # src/web assessment-dashboard route tests
httpx>=0.27               # fastapi TestClient
python-multipart>=0.0.19  # src/web upload routes (UploadFile / Form) — E11.24/E11.26
pytest-playwright>=0.5     # tests/browser/ — Playwright harness (E13.15); skipped unless BROWSER=1, needs `playwright install chromium`
uvicorn>=0.30             # tests/browser/serve.py runs the app for the browser harness
